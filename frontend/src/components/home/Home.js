import React from 'react';
import Filter from '../filter/Filter';
import SearchBox from '../searchBox/SearchBox'

const Home = () => {
  return (
    <div className="App">
      Home ROUTE
      <Filter/>
      <SearchBox/>
    </div>
  );
}

export default Home;
